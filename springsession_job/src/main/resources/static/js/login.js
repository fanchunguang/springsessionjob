/**
 * 提交登录请求
 */
function login(){

    /*let username = $(this).attr("username").value;
    let password = $(this).attr("password").value;
    let postData = {
        username: username,
        password: password
    };
    $.post({
        url: 'login.do',
        type: 'post',
        dataType: 'json',
        data: postData,
        success: function (res) {

        }
    })*/

}